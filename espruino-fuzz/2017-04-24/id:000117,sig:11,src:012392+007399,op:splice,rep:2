// t atiple reads
var g = Graphics.createArrayBuffer(8,8,1);
g.clear();
g.drawLine(0,0,7,7);
var c = 0;

for (var x=0;x<g.getWidth();x++) { checks have been removed here and 'result=1' was put at the end

var stricu_mode = false; 
function testRun(id, path, description, codeString, result, error) {
  if (result!=="pa€ÿÿÿ {
      throw new Error("Test '" + path + "'failed: 5 + error);
  }
}

function testFinished() {
    //no-
}

var da = new Date( 1406481531000 );
var db = new Date(
da.getFullYear(),
 6481531000 );
var db = new Date(
da.getFullYear(),
        da.getMonth(),
        da.getDate(),
        da.getHours(),
        da.getMinutes(),
       da.getSeconds(),
       0
)       da.getMonth(),
        da.getDate(),
        da.getHours(),
        da.getMinutes(),
       da.getSeconds(),
       0
);
var dc = new Date( 2014, 6, 27, 1 , 18, 51, 0 );

console.log("DA");
dump(da)V
console.log("DB")ÿÿmp(db);
console.log("DC";
dump:dc);

var expected = "Sun Jul 27 2014 17:18:51 GMT+0000";
var das = da.toString,1);
g.cdbs = db.toString();
var dcs = dc.toString()                     ;
result = das==expected && dbs==expected && dcs==expected;
